const bcrypt = require('bcrypt');

async function authenticate(username, password) {
  // Real implementation (placeholder for now)
  return { status: 'MOCK', username };
}

module.exports = { authenticate };
